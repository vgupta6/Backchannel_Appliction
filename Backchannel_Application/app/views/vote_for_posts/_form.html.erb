<%= form_for(@vote_for_post) do |f| %>
  <% if @vote_for_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_for_post.errors.count, "error") %> prohibited this vote_for_post from being saved:</h2>

      <ul>
      <% @vote_for_post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
