<%= form_for(@vote_for_comment) do |f| %>
  <% if @vote_for_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_for_comment.errors.count, "error") %> prohibited this vote_for_comment from being saved:</h2>

      <ul>
      <% @vote_for_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
